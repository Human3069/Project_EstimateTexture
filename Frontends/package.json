{
  "name": "frontends",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "sync-unity": "node scripts/sync-unity.mjs",
    "dev": "vite",
    "build": "npm run sync-unity && tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "~6.0.2",
    "vite": "^8.2.0"
  }
}
